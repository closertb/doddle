import { ActionsGroup, HFormCustomAction } from '../types';
/**
 * 创建 actions
 * @param actions 这里需要直接修改 actions
 * @param props Custom 的 props
 * @param form form
 * @param propsRef ref
 * @returns
 */
export declare function createAction(actions: ActionsGroup, props: any, propsRef: any): HFormCustomAction;
